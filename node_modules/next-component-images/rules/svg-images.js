// =============================================================================================================================
// RULES - SVG IMAGES
// =============================================================================================================================
module.exports = (svgoOptions = {}) => ({
  test: /\.svg$/,
  use: [
    {
      loader: "react-svg-loader",
      options: {
        jsx: false,
        svgo: svgoOptions,
      },
    },
  ],
});
